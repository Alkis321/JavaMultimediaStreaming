# Video Streaming Project - Difficulty Levels Analysis

## 1. Basic Level (Mandatory)

- **Java Sockets**
  - Server maintains video list (name, format, resolution)
  - Client requests/receives list
  - *Challenge:* Socket state management

- **FFmpeg Processing**
  - Convert to 3 formats (AVI, MP4, MKV) + 5 resolutions
  - *Challenge:* FFmpeg-Java integration

- **Basic GUI (JavaFX)**
  - Video list + resolution selector
  - *Challenge:* Thread-safe UI updates

## 2. Intermediate Level

- **Multi-Client Support**
  - Thread-per-client model
  - *Challenge:* Resource synchronization

- **Caching/Buffering**
  - Pre-load 5-10% of video
  - *Challenge:* Byte-range requests

- **Protocol Selection**
  - TCP/UDP/RTP implementations
  - *Challenge:* UDP packet loss

## 3. Advanced Level

- **Adaptive Streaming**
  - Dynamic resolution switching
  - *Challenge:* Mid-stream transitions

- **Load Balancer**
  - Distribute clients across servers
  - *Challenge:* Session handoff

- **Recording**
  - Client-side stream saving
  - *Challenge:* A/V sync
  


  ffmpeg -i mp4_example-480p.mp4 -filter_complex "[0:v]split=3[v1][v2][v3];[v1]scale=w=1920:h=1080[v1out];[v2]scale=w=1280:h=720[v2out];[v3]scale=w=854:h=480[v3out]" -map "[v1out]" -c:v:0 libx264 -b:v:0 5000k -map 0:a -c:a:0 aac -b:a:0 128k -map "[v2out]" -c:v:1 libx264 -b:v:1 3000k -map 0:a -c:a:1 aac -b:a:1 128k -map "[v3out]" -c:v:2 libx264 -b:v:2 1000k -map 0:a -c:a:2 aac -b:a:2 128k -f hls -var_stream_map "v:0,a:0 v:1,a:1 v:2,a:2" -hls_time 4 -hls_playlist_type vod -master_pl_name master.m3u8 -hls_segment_filename "output/stream_%v/segment_%03d.ts" "output/stream_%v/playlist.m3u8"